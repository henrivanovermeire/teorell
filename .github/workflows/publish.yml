name: Publish to PyPI

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/teorell-core
    permissions:
      id-token: write  # Trusted Publishing (OIDC) — no PyPI password in GitHub

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Verify tag matches package version
        run: |
          set -euo pipefail
          TAG="${GITHUB_REF_NAME}"
          TAG="${TAG#v}"
          PKG="$(python -c 'import tomllib; print(tomllib.load(open("pyproject.toml","rb"))["project"]["version"])')"
          echo "GitHub release tag (stripped): ${TAG}"
          echo "pyproject.toml version:       ${PKG}"
          if [[ "${TAG}" != "${PKG}" ]]; then
            echo "::error::Release tag v${PKG} required (got '${GITHUB_REF_NAME}'). Bump version in pyproject.toml to match."
            exit 1
          fi

      - name: Run tests
        run: pytest -q

      - name: Build sdist and wheel
        run: |
          python -m build
          twine check dist/*
          # Sanity: wheel must not ship the web UI
          python - <<'PY'
          from pathlib import Path
          import zipfile
          wheels = list(Path("dist").glob("*.whl"))
          assert wheels, "no wheel built"
          names = zipfile.ZipFile(wheels[0]).namelist()
          bad = [n for n in names if n.startswith("web/") or "/web/" in n]
          assert not bad, f"web UI leaked into wheel: {bad[:10]}"
          print("wheel ok:", wheels[0].name, f"({len(names)} files)")
          PY

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
